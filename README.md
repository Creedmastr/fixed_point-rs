# Informations

This project is a library for using fixed-point numbers, and it works kinda well.
Basic operations are implemented, and just use `.fmt()` for getting the floated version of the number.